[build]
  command = "npm ci && npm run build"
  publish = "dist"  # adjust to your build output folder

[build.environment]
  NODE_VERSION = "20"

[build.processing]
  skip_processing = false

[[plugins]]
  package = "@netlify/plugin-install-rust"
